# Virtual store em caminho absoluto curto + nomes de pasta limitados:
# evita estourar o limite de path do Windows/CMake (CMAKE_OBJECT_PATH_MAX)
# ao compilar módulos nativos (ex.: react-native-worklets) no build Android.
virtual-store-dir=C:\ps
virtual-store-dir-max-length=30
