# OneClick ERP — binarios do app mobile

Coloque aqui o APK gerado pelo EAS (`eas build -p android --profile preview`).
O controller /api/mobile-app serve o .apk mais recente desta pasta.

iOS: nao ha instalador self-hosted; defina MOBILE_IOS_URL (TestFlight/App Store).
