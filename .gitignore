# Dependencies
node_modules/

# Build outputs
dist/
.next/
.turbo/

# Environment
.env
.env.local
.env.*.local

# MCP — pode conter tokens (Hostinger, etc.)
.mcp.json

# Credenciais de deploy (SSH, registry, etc.)
.deploy.local
.deploy-*.local
*.pem
*.ppk

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Prisma
packages/db/src/generated/

# Logs
*.log
pnpm-debug.log*

# Testing
coverage/

# Uploads (certificados, arquivos de usuários)
apps/api/uploads/
!apps/api/uploads/.gitkeep

# PEM/PFX files (exceto pasta certs/ que é versionada)
apps/api/uploads/*.pem
apps/api/uploads/*.pfx

# Credenciais Google (Service Account, OAuth tokens, etc.)
/google/

# Claude Code — personal/local agent files (skills, worktrees, local settings)
.claude/skills/
.claude/worktrees/
.claude/settings.local.json
.claude/plans/

# Build artifacts e binários grandes
OneClick-ERP.exe
*.exe
*.dll

# Backups e dumps (NÃO versionar — usar S3/Drive)
*.bak
*.dump
*.sql.gz
_backups/
apps/web/_backups/
dump-deploy.dump

# Legado v1 (referência, não versionar)
/modelo/

# Screenshots / capturas
/prints/

# Razao frames (gerados pelo launcher)
scripts/launcher/razao-frames/

# Next.js build cache
**/tsconfig.tsbuildinfo
**/next-env.d.ts

# Tmp / diagnósticos
packages/db/tmp/
